import Post from "../../shared/models/Post";

interface IProps {
  posts: Post[];
}

const PostsDisplayed: React.FunctionComponent<IProps> = (props: IProps) => {
  return <div></div>;
};

export default PostsDisplayed;
